/*

double Dmax = 25б k = 0.5;
int D = 1;
void tail_sin() {
  for(double state[i] = start[5]; Talpha < finish[5] + 1; Talpha++) {
    a[5].write(Talpha);
    delay(int(Dmax*(1.0 - k*sin(0.0175*180* (state[i] - start[i])/(finish[i] - start[i])))));
  }
  delay(D);
  for(double Talpha = Tmax; Talpha > Tmin - 1; Talpha--) {
    a[5].write(int(Talpha));
    delay(int(Dmax*(1.0 - k*sin(0.0175*180* (state[i] - start[i])/(finish[i] - start[i])))));
    }
  delay(D);
 
}

*/
